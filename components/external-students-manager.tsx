"use client";

import { useEffect, useState } from "react";
import { Users, RefreshCw } from "lucide-react";

type Student = { id: number; name: string; email?: string | null; status?: string | null; className?: string | null };
export function ExternalStudentsManager() { const [students, setStudents] = useState<Student[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const load = async () => { setLoading(true); try { const response = await fetch("/api/professor/external-classes?mode=students", { cache: "no-store" }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error || "Não foi possível carregar os alunos."); setStudents(payload.students || (payload.classes || []).flatMap((item: { students?: Array<Student>; className?: string }) => (item.students || []).map((student) => ({ ...student, className: student.className || item.className }))) || payload.data || []); } catch (cause) { setError(cause instanceof Error ? cause.message : "Não foi possível carregar os alunos."); } finally { setLoading(false); } }; useEffect(() => { const timer = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(timer); }, []); return <section className="space-y-5"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-red-600">Cadastro</p><h2 className="mt-1 text-2xl font-black">Alunos externos</h2><p className="mt-1 text-sm text-muted-foreground">Consulte os alunos sem misturar a rotina de cada turma.</p></div><button onClick={() => void load()} className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-border px-3 text-sm font-bold hover:bg-muted"><RefreshCw size={15} /> Atualizar</button></div>{error && <p role="alert" className="rounded-xl bg-red-50 p-4 text-sm font-semibold text-red-700">{error}</p>}{loading ? <div className="rounded-2xl border border-border bg-card p-8 text-sm text-muted-foreground">Carregando alunos...</div> : <div className="overflow-hidden rounded-2xl border border-border bg-card">{students.length ? students.map((student) => <div key={student.id} className="flex items-center justify-between border-b border-border p-4 last:border-0"><div className="flex items-center gap-3"><Users size={18} className="text-red-600" /><div><p className="font-bold">{student.name}</p><p className="text-sm text-muted-foreground">{student.email || "Sem e-mail"}{student.className ? ` · ${student.className}` : ""}</p></div></div><span className="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-700">{student.status || "Ativo"}</span></div>) : <div className="p-10 text-center text-sm text-muted-foreground">Nenhum aluno encontrado.</div>}</div>}</section>; }
