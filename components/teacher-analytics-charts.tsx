import { BarChart3, TrendingUp, Users } from "lucide-react";

interface TeacherAnalyticsChartsProps { totalStudents: number; activeStudents: number; averageProgress: number | null; totalEnrollments: number; }

export function TeacherAnalyticsCharts({ totalStudents, activeStudents, averageProgress, totalEnrollments }: TeacherAnalyticsChartsProps) {
  const progressWidth = averageProgress === null ? 0 : Math.min(100, Math.max(0, averageProgress));
  return <div className="mb-8 grid grid-cols-1 gap-6 md:grid-cols-3"><div className="surface-card space-y-3 p-6"><div className="flex items-center justify-between text-muted-foreground"><span className="text-xs font-black uppercase tracking-wider">Engajamento de Alunos</span><Users size={18} className="text-red-600" /></div><div className="flex items-baseline justify-between"><p className="text-3xl font-black text-foreground">{activeStudents} <span className="text-sm font-normal text-muted-foreground">/ {totalStudents}</span></p><span className="rounded-full bg-emerald-50 px-2 py-0.5 text-xs font-bold text-emerald-600">Ativos</span></div><div className="h-2.5 w-full overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-emerald-600 transition-all duration-500" style={{ width: `${totalStudents ? Math.round((activeStudents / totalStudents) * 100) : 0}%` }} /></div><p className="text-xs text-muted-foreground">Alunos com ao menos uma atividade ou progresso registrado.</p></div><div className="surface-card space-y-3 p-6"><div className="flex items-center justify-between text-muted-foreground"><span className="text-xs font-black uppercase tracking-wider">Progresso Médio Geral</span><TrendingUp size={18} className="text-red-600" /></div><div className="flex items-baseline justify-between"><p className="text-3xl font-black text-foreground">{averageProgress === null ? "—" : `${averageProgress}%`}</p><span className="rounded-full bg-red-50 px-2 py-0.5 text-xs font-bold text-red-600">Registros reais</span></div><div className="h-2.5 w-full overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-red-600 transition-all duration-500" style={{ width: `${progressWidth}%` }} /></div><p className="text-xs text-muted-foreground">Média calculada apenas sobre matrículas com progresso persistido.</p></div><div className="surface-card space-y-3 p-6"><div className="flex items-center justify-between text-muted-foreground"><span className="text-xs font-black uppercase tracking-wider">Matrículas registradas</span><BarChart3 size={18} className="text-red-600" /></div><div className="flex items-baseline justify-between"><p className="text-3xl font-black text-foreground">{totalEnrollments}</p><span className="rounded-full bg-blue-50 px-2 py-0.5 text-xs font-bold text-blue-600">Banco</span></div><div className="h-2.5 w-full overflow-hidden rounded-full bg-muted"><div className="h-full w-full rounded-full bg-blue-600 transition-all duration-500" /></div><p className="text-xs text-muted-foreground">Matrículas visíveis para o escopo deste professor.</p></div></div>;
}
