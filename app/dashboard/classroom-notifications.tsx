"use client";

/**
 * O banner permanece como ponto de extensão, mas não exibe status inventado.
 * A integração Classroom só deve aparecer quando houver uma resposta real e
 * autorizada da API; a sessão atual não possui esse escopo.
 */
export function ClassroomGradesNotificationBanner() {
  return null;
}
