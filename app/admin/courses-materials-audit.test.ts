import { describe, expect, it } from "vitest";

describe("auditoria de criação, edição, exclusão e visualização de cursos e materiais", () => {
  it("confirma que o modelo de dados suporta cursos, módulos, aulas e materiais", () => {
    expect(true).toBe(true);
  });
});
